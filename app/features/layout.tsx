import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Features for CBC Teachers',
  description: 'Explore Mwalimu AI learning modules, AI coaching, resources, and teacher community features.',
}

export default function FeaturesLayout({ children }: { children: React.ReactNode }) { return children }
